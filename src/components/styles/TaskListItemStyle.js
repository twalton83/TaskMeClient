import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  root: {
   
  },
  checkButton: {
   "&:hover": {
     fill: "#eb5e28"
   }
 }

}));

export default useStyles
